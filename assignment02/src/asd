/*
Benedict Hofmockel
4/12/2022
*/

#include <iostream>
#include <random>
#include <ctime>
#include <cstdlib>

using namespace std;



int main(int argc, char** argv){
    cout << "The commandline argument provided was " << argv[1] << endl;

    std::random_device rd;
    mt19937 mt(rd);
    int guestPick = (mt() % 3)+1;

    const char *objectsBehindDoors[3] = {"Goat", "Car", "Goat"};
    std::shuffle(objectsBehindDoors, objectsBehindDoors + sizeof(objectsBehindDoors) / sizeof(objectsBehindDoors[0]), mt);

    // cout << "this is a random number: " << mt() << '\n';

    // cout << "this is another random number: " << (mt() % 2)+1 << '\n';
    // cout << "this is another random number: " << (mt() % 2)+1 << '\n';
    // cout << "this is another random number: " << (mt() % 2)+1 << '\n';
    // cout << "this is another random number: " << (mt() % 2)+1 << '\n';
    // cout << "this is another random number: " << (mt() % 2)+1 << '\n';

    return 0;
}